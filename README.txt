Criei uma aplicação react single page utilizando o create react app, usando a biblioteca bootstrap;
Com isso montei o modal com a ficha de cadastro dos imoveis, com seus respectivos campos, e preenchendo os campos e clicando no botao de salvar, insere o elemento na lista e ja aparece na tabela;
Travei na parte de inserir o elemento na lista; Ele até chega a inserir, mas quando vai chamar a função .map pra mostrar os imóveis, ele diz que o elemento nao é do tipo lista..
Não cheguei a fazer o filtro e utilizar a API para pegar as cidades por falta de tempo.